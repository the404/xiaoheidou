@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@model EasyWeixin.Model.ResponseImage
<div class="main-wrapper">
    <div class="container-fluid">
        <div class="row-fluid ">
            <div class="span12">
                <div class="primary-head">
                    <h3 class="page-header">图片管理</h3>
                    <ul class="top-right-toolbar">
                        <li><a data-toggle="dropdown" class="dropdown-toggle blue-violate" href="#" title="Users"><i class="icon-user"></i></a>
                        </li>
                        <li><a href="#" class="green" title="Upload"><i class=" icon-upload-alt"></i></a></li>
                        <li><a href="#" class="bondi-blue" title="Settings"><i class="icon-cogs"></i></a></li>
                    </ul>
                </div>
                <ul class="breadcrumb">
                    <li><a href="#" class="icon-home"></a><span class="divider "><i class="icon-angle-right"></i></span></li>
                    <li><a href="#">图片管理</a><span class="divider"><i class="icon-angle-right"></i></span></li>
                    <li class="active">图片创建</li>
                </ul>
            </div>
        </div>

        @{ using (Html.BeginForm("Edit", "ResponseImage"))
           {
                @Html.HiddenFor(model => model.ResponseImageID)
                @Html.HiddenFor(model => model.ImageUrl)

            <div class="row-fluid">
                <div class="span12">
                    <div class="content-widgets gray">
                        <div class="widget-head bondi-blue">
                            <h3>编辑图片</h3>
                        </div>
                        <div class="widget-container">
                            <div class="form-container grid-form form-background">
                                <!-- our error container -->
                                <div class="control-group">
                                    <label class="control-label">图片 </label>
                                    <div class="controls">
                                        <img src="@Model.ImageUrl" />
                                    </div>
                                </div>
                                <div class="control-group">
                                    <label class="control-label">图片名称修改</label>
                                    <div class="controls">
                                        @Html.TextBoxFor(model => model.ImageName, new { @placeholder = "输入图片名称", @class = "span7" })
                                    </div>
                                    <label class="error">@TempData["ErrorMessage"] </label>
                                </div>
                                <div class="form-actions">
                                    <button type="submit" class="btn btn-extend">提交</button>
                                    @Html.ActionLink("返回列表", "Index", new { }, new { @class = "btn btn-extend" })
                                </div>
                                <div class="error-container">
                                    <h4></h4>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
           }
        }
    </div>
</div>